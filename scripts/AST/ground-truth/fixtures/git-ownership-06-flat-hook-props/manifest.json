{
  "tool": "ownership",
  "created": "2026-03-16",
  "source": "synthetic",
  "files": ["EntityManager.tsx"],
  "expectedClassifications": [
    {
      "file": "EntityManager.tsx",
      "line": 15,
      "symbol": "EntityManager",
      "expectedKind": "CONTAINER",
      "notes": "Receives hooks via flat props (useEntityQuery, useCreateMutation) and calls them. The hooks interpreter classifies these as LIKELY_SERVICE_HOOK via convention (low confidence). The 'multiple service hooks' signal (2+ LIKELY_SERVICE_HOOK) counts as an additional container signal, giving 2 total (has service hooks + multiple service hooks) => CONTAINER medium."
    }
  ],
  "status": "calibrated"
}

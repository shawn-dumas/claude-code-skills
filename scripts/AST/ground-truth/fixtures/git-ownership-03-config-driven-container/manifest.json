{
  "tool": "ownership",
  "created": "2026-03-16",
  "source": "git-production",
  "files": ["UsageContainer.tsx"],
  "expectedClassifications": [
    {
      "file": "UsageContainer.tsx",
      "line": 14,
      "symbol": "UsageContainer",
      "expectedKind": "CONTAINER",
      "notes": "Container suffix + useQueryStates (router hook) = 2 signals. Service hooks come from config props (useKpiQuery, useDetailQuery) so the hooks interpreter classifies them as UNKNOWN_HOOK. Interpreter reaches CONTAINER/medium via the 2 visible signals, which is correct."
    }
  ],
  "status": "calibrated"
}

---
name: synth-unclosed-frontmatter
description: A skill file with an opening frontmatter delimiter but no closing one.

# synth-unclosed-frontmatter

Content without a properly closed frontmatter block.

## Step 1: Action

Run the command:

```bash
pnpm build
```

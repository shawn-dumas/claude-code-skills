{
  "tool": "ownership",
  "created": "2026-03-16",
  "source": "git-production",
  "files": ["SettingsEntityContainer.tsx"],
  "expectedClassifications": [
    {
      "file": "SettingsEntityContainer.tsx",
      "line": 14,
      "symbol": "SettingsEntityContainer",
      "expectedKind": "CONTAINER",
      "notes": "Named SettingsEntityContainer (Container suffix = 1 signal). Service hooks come from config props (useGetAllQuery, useCreateMutation) so the hooks interpreter sees them as UNKNOWN_HOOK rather than LIKELY_SERVICE_HOOK. Only 1 container signal (suffix) is insufficient for CONTAINER classification. Interpreter says DDAU_COMPONENT but this is a data-orchestrating container."
    }
  ],
  "status": "pending"
}
